# Psycopg2 accepts standard URLs only (no ?schema=... — use default schema public).
# Matches docker-compose.yml (PostgreSQL exposed on host port 5434).
DATABASE_URL=postgresql://postgres:postgres@localhost:5434/fastapi
PROJECT_NAME=Student API
